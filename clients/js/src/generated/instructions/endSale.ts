/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { MALLOW_JELLYBEAN_PROGRAM_ADDRESS } from '../programs';

export const END_SALE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  37, 239, 52, 17, 120, 44, 213, 125,
]);

export function getEndSaleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(END_SALE_DISCRIMINATOR);
}

export type EndSaleInstruction<
  TProgram extends string = typeof MALLOW_JELLYBEAN_PROGRAM_ADDRESS,
  TAccountJellybeanMachine extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountJellybeanMachine extends string
        ? WritableAccount<TAccountJellybeanMachine>
        : TAccountJellybeanMachine,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type EndSaleInstructionData = { discriminator: ReadonlyUint8Array };

export type EndSaleInstructionDataArgs = {};

export function getEndSaleInstructionDataEncoder(): FixedSizeEncoder<EndSaleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: END_SALE_DISCRIMINATOR })
  );
}

export function getEndSaleInstructionDataDecoder(): FixedSizeDecoder<EndSaleInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getEndSaleInstructionDataCodec(): FixedSizeCodec<
  EndSaleInstructionDataArgs,
  EndSaleInstructionData
> {
  return combineCodec(
    getEndSaleInstructionDataEncoder(),
    getEndSaleInstructionDataDecoder()
  );
}

export type EndSaleInput<
  TAccountJellybeanMachine extends string = string,
  TAccountAuthority extends string = string,
> = {
  /** Gumball machine account. */
  jellybeanMachine: Address<TAccountJellybeanMachine>;
  /** Gumball Machine authority. This is the address that controls the upate of the jellybean machine. */
  authority: TransactionSigner<TAccountAuthority>;
};

export function getEndSaleInstruction<
  TAccountJellybeanMachine extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof MALLOW_JELLYBEAN_PROGRAM_ADDRESS,
>(
  input: EndSaleInput<TAccountJellybeanMachine, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): EndSaleInstruction<
  TProgramAddress,
  TAccountJellybeanMachine,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_JELLYBEAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    jellybeanMachine: {
      value: input.jellybeanMachine ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('jellybeanMachine', accounts.jellybeanMachine),
      getAccountMeta('authority', accounts.authority),
    ],
    data: getEndSaleInstructionDataEncoder().encode({}),
    programAddress,
  } as EndSaleInstruction<
    TProgramAddress,
    TAccountJellybeanMachine,
    TAccountAuthority
  >);
}

export type ParsedEndSaleInstruction<
  TProgram extends string = typeof MALLOW_JELLYBEAN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Gumball machine account. */
    jellybeanMachine: TAccountMetas[0];
    /** Gumball Machine authority. This is the address that controls the upate of the jellybean machine. */
    authority: TAccountMetas[1];
  };
  data: EndSaleInstructionData;
};

export function parseEndSaleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedEndSaleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      jellybeanMachine: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getEndSaleInstructionDataDecoder().decode(instruction.data),
  };
}
