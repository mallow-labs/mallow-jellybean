/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { resolveAuthorityPda } from '../../hooked';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RemoveCoreItemInstructionAccounts = {
  /** Gumball Machine account. */
  jellybeanMachine: PublicKey | Pda;
  authorityPda?: PublicKey | Pda;
  /** Authority of the jellybean machine. */
  authority?: Signer;
  asset?: PublicKey | Pda;
  /** Core asset's collection if it's part of one. */
  collection?: PublicKey | Pda;
  mplCoreProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type RemoveCoreItemInstructionData = {
  discriminator: Uint8Array;
  index: number;
};

export type RemoveCoreItemInstructionDataArgs = { index: number };

export function getRemoveCoreItemInstructionDataSerializer(): Serializer<
  RemoveCoreItemInstructionDataArgs,
  RemoveCoreItemInstructionData
> {
  return mapSerializer<
    RemoveCoreItemInstructionDataArgs,
    any,
    RemoveCoreItemInstructionData
  >(
    struct<RemoveCoreItemInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['index', u8()],
      ],
      { description: 'RemoveCoreItemInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([146, 20, 197, 215, 49, 62, 130, 92]),
    })
  ) as Serializer<
    RemoveCoreItemInstructionDataArgs,
    RemoveCoreItemInstructionData
  >;
}

// Args.
export type RemoveCoreItemInstructionArgs = RemoveCoreItemInstructionDataArgs;

// Instruction.
export function removeCoreItem(
  context: Pick<Context, 'eddsa' | 'identity' | 'payer' | 'programs'>,
  input: RemoveCoreItemInstructionAccounts & RemoveCoreItemInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mallowJellybean',
    'J3LLYcm8V5hJRzCKENRPW3yGdQ6xU8Nie8jr3mU88eqq'
  );

  // Accounts.
  const resolvedAccounts = {
    jellybeanMachine: {
      index: 0,
      isWritable: true as boolean,
      value: input.jellybeanMachine ?? null,
    },
    authorityPda: {
      index: 1,
      isWritable: true as boolean,
      value: input.authorityPda ?? null,
    },
    authority: {
      index: 2,
      isWritable: true as boolean,
      value: input.authority ?? null,
    },
    asset: {
      index: 3,
      isWritable: true as boolean,
      value: input.asset ?? null,
    },
    collection: {
      index: 4,
      isWritable: true as boolean,
      value: input.collection ?? null,
    },
    mplCoreProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.mplCoreProgram ?? null,
    },
    systemProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveCoreItemInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authorityPda.value) {
    resolvedAccounts.authorityPda = {
      ...resolvedAccounts.authorityPda,
      ...resolveAuthorityPda(
        context,
        resolvedAccounts,
        resolvedArgs,
        programId,
        true
      ),
    };
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.mplCoreProgram.value) {
    resolvedAccounts.mplCoreProgram.value = context.programs.getPublicKey(
      'mplCore',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.mplCoreProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRemoveCoreItemInstructionDataSerializer().serialize(
    resolvedArgs as RemoveCoreItemInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
